$("#card").wScratchPad({
  size: 100, // The size of the brush/scratch.
  bg: `Images/img1.png`, // Background (image path or hex color).
  fg: `Images/front.png`, // Foreground (image path or hex color).
  cursor: "pointer", // Set cursor.
});
